[project]
name = "og1-rebuild-tools"
version = "0.1.0"
requires-python = ">=3.12,<3.15"
dependencies = []

[tool.poetry]
package-mode = false
[dependency-groups]
dev = [
    "pre-commit (>=4.6.2,<5.0.0)"
]
